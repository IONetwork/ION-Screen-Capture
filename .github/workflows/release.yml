name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

# The npm project lives in src/ (package.json + src-tauri there).
defaults:
  run:
    shell: pwsh
    working-directory: src

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0

      - name: Setup Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: src/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/src-tauri

      - name: Install dependencies
        run: npm ci

      # `npm run tauri` runs from src/ (its package root), so the CLI finds
      # src-tauri. This builds the frontend (beforeBuildCommand) + Rust app and
      # bundles the NSIS setup.exe and MSI.
      - name: Build installers + app
        run: npm run tauri build

      # Create (or reuse) a DRAFT GitHub Release and attach the NSIS installer,
      # MSI, and the lean portable exe (the release binary, renamed).
      - name: Publish draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $ver = (Get-Content src-tauri/tauri.conf.json -Raw | ConvertFrom-Json).version
          $tag = "v$ver"
          $rel = "src-tauri/target/release"
          $portable = "ION-Screen-Capture_${ver}_x64-portable.exe"
          Copy-Item "$rel/ion-screen-capture.exe" $portable
          $assets = @(
            (Get-ChildItem "$rel/bundle/nsis/*-setup.exe" | Select-Object -First 1).FullName,
            (Get-ChildItem "$rel/bundle/msi/*.msi"        | Select-Object -First 1).FullName,
            (Resolve-Path $portable).Path
          )
          gh release view $tag *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $tag --draft --title "ION Screen Capture $tag" --notes "Automated build."
          }
          gh release upload $tag $assets --clobber
